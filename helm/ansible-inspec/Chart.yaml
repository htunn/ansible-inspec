apiVersion: v2
name: ansible-inspec
description: Enterprise compliance testing with Ansible and InSpec integration
type: application
version: 0.2.16
appVersion: "0.2.11"

keywords:
  - compliance
  - security
  - ansible
  - inspec
  - devops
  - audit

maintainers:
  - name: ansible-inspec contributors
    email: htunnthuthu.linux@gmail.com

sources:
  - https://github.com/Htunn/ansible-inspec

dependencies:
  - name: postgresql
    version: "18.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled

home: https://github.com/Htunn/ansible-inspec

annotations:
  artifacthub.io/category: security
  artifacthub.io/license: GPL-3.0-or-later
  artifacthub.io/readme: |
    # Ansible InSpec Helm Chart

    This Helm chart deploys the Ansible InSpec server with PostgreSQL database on Kubernetes.

    ## Overview

    Ansible InSpec is a compliance and security automation tool that integrates InSpec profiles with Ansible workflows. It provides:

    - **REST API Server**: FastAPI-based server with 30+ endpoints
    - **Database Backend**: PostgreSQL with Prisma ORM
    - **Authentication**: Azure AD OAuth2, local password auth, JWT tokens
    - **RBAC**: Admin, operator, and viewer roles
    - **VCS Integration**: GitHub, GitLab with webhook support
    - **Job Management**: Execute InSpec profiles via Ansible
    - **Security**: Encrypted credentials, secure secrets management

    ## Prerequisites

    - Kubernetes 1.19+
    - Helm 3.0+
    - PV provisioner support in the underlying infrastructure (for persistent storage)
    - PostgreSQL 12+ (if using external database)

    ## Installation

    ### Basic Installation

    ```bash
    helm install ansible-inspec ./ansible-inspec
    ```

    ### Installation with Custom Values

    ```bash
    helm install ansible-inspec ./ansible-inspec \
      --set image.tag=0.2.11 \
      --set postgresql.auth.password=changeme \
      --set secrets.jwtSecret=your-jwt-secret \
      --set secrets.encryptionKey=your-encryption-key \
      --set secrets.adminPassword=your-admin-password
    ```

    ### Installation from Repository

    ```bash
    helm repo add ansible-inspec https://htunn.github.io/ansible-inspec/helm
    helm install ansible-inspec ansible-inspec/ansible-inspec
    ```

    ## Configuration

    The following table lists the configurable parameters and their default values.

    ### Common Parameters

    | Parameter | Description | Default |
    |-----------|-------------|---------|
    | `replicaCount` | Number of replicas | `2` |
    | `image.repository` | Image repository | `ghcr.io/htunn/ansible-inspec` |
    | `image.tag` | Image tag | `0.2.11` |
    | `image.pullPolicy` | Image pull policy | `IfNotPresent` |
    | `service.type` | Service type | `ClusterIP` |
    | `service.port` | Service port | `8080` |

    For complete documentation, see: https://github.com/Htunn/ansible-inspec
  artifacthub.io/changes: |
    - kind: fixed
      description: Auto-update existing admin users with hashed passwords on startup
      links:
        - name: GitHub Release
          url: https://github.com/Htunn/ansible-inspec/releases/tag/v0.2.11
    - kind: fixed
      description: Fix SSH key authentication for VCS private repositories
    - kind: changed
      description: Improve SSH key handling with proper flush and fsync
    - kind: changed
      description: Update to application version 0.2.11
    - kind: added
      description: Add secure password authentication with bcrypt hashing
      links:
        - name: GitHub Release
          url: https://github.com/Htunn/ansible-inspec/releases/tag/v0.2.9
    - kind: added
      description: Add configurable admin credentials via Helm values (adminUsername, adminPassword, adminEmail, adminName)
    - kind: changed
      description: Update Prisma schema with hashedPassword field for secure password storage
    - kind: changed
      description: Replace passlib with native bcrypt due to compatibility with Python 3.13
    - kind: fixed
      description: Fix password verification to properly reject incorrect passwords
    - kind: fixed
      description: Fix Prisma query engine binary path configuration in Docker Compose
    - kind: security
      description: Remove insecure 'any password' authentication fallback
    - kind: changed
      description: Update to application version 0.2.9
    - kind: fixed
      description: Fix Streamlit password login authentication flow
      links:
        - name: GitHub Release
          url: https://github.com/Htunn/ansible-inspec/releases/tag/v0.2.8
    - kind: fixed
      description: Fix OAuth subdomain routing for multi-domain deployments
    - kind: fixed
      description: Fix service selector conflicts between API and UI pods
    - kind: changed
      description: Add component-based pod labeling for proper service routing
    - kind: changed
      description: Update to application version 0.2.8
  artifacthub.io/containsSecurityUpdates: "true"
  artifacthub.io/prerelease: "false"
  artifacthub.io/images: |
    - name: ansible-inspec
      image: ghcr.io/htunn/ansible-inspec:0.2.11
      platforms:
        - linux/amd64
        - linux/arm64
    - name: postgresql
      image: docker.io/bitnami/postgresql:15
      whitelisted: true
  artifacthub.io/links: |
    - name: Chart Source
      url: https://github.com/Htunn/ansible-inspec/tree/main/helm/ansible-inspec
    - name: Documentation
      url: https://github.com/Htunn/ansible-inspec-docs
    - name: GitHub Container Registry
      url: https://github.com/Htunn/ansible-inspec/pkgs/container/ansible-inspec
    - name: PyPI Package
      url: https://pypi.org/project/ansible-inspec/
    - name: support
      url: https://github.com/Htunn/ansible-inspec/issues
  artifacthub.io/maintainers: |
    - name: Htunn Thu Thu
      email: htunnthuthu.linux@gmail.com
  artifacthub.io/recommendations: |
    - url: https://artifacthub.io/packages/helm/bitnami/postgresql
    - url: https://artifacthub.io/packages/helm/prometheus-community/kube-prometheus-stack

